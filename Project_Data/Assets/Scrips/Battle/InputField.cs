using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

/*
public class InputField : MonoBehaviour
{ 
    public Text inputField_num;
    int Min;
    int Max;
    int NumberToClamp;

    public void ValueChange(int value)
    {
        NumberToClamp = Mathf.Clamp(value, Min, Max);
        inputField_num.text = NumberToClamp.ToString();
    }
}
*/